# RSCD Replicator — Shaikh (2016) Replication v1.0
# Python 3.11+
#
# Install in a clean venv:
#     python -m venv .venv
#     .venv/Scripts/activate    (Windows)   or   source .venv/bin/activate (Unix)
#     pip install -r requirements.txt

# Core data
pandas>=2.1
numpy>=1.26
pyarrow>=14.0
openpyxl>=3.1
PyYAML>=6.0

# HTTP / APIs
requests>=2.31
fredapi>=0.5.1

# Optional: visualization replay (not required by replicate.py itself)
# plotly>=5.18
# dash>=2.14
# dash-bootstrap-components>=1.5
