# S1301 — Extension Provenance Record

**Phase**: 6 (Extension)
**Construction**: `formula`
**Content type**: `theoretical`
**Extension method**: `not_applicable_theoretical`
**Status v1**: complete (no extension to perform)

---

## 1. Why this series cannot be extended

Fig 13.7 is an analytical schematic with no calendar time axis and no
observed data. There is nothing to "extend" — re-running the loader with a
new seed produces a different realisation of the same mathematical object,
not a continuation of an observed series.

Per anu-framework: "Extensions ONLY apply to time_series type." S1301 is
classified `theoretical`; extension is `not_applicable_theoretical`.

## 2. Parameter disclosure (required for theoretical series)

The parameter values used to render the canonical S1301 realisation are
declared in the loader and DPR:

- `ln Y0 = 8.0`
- `alpha = 0.03`
- `sigma_epsilon = 0.015`
- `seed = 42`
- `t_max = 75`

Shaikh does not publish the parameter values used to draw Fig 13.7; these are
the RSCD authors' choices, documented here to satisfy the No-Proxies-Without-
Disclosure rule by construction.

## 3. No-Proxy / No-Synthetic

By definition, all values are synthetic (drawn from a declared random
process). This is acceptable for `theoretical` content_type because the
parameters are fully disclosed.

## 4. CD2 divergence pre-disclosure

No CD/CD2 predecessor. CD2's S102 was incorrectly named "IRS 2011 Income
Distribution"; that lineage is captured in RSCD as S1702 and S1703. The
S102 ID is not reused.
